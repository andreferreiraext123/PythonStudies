# Obtendo valores fracionarios dentro de uma determinada fixa, podemos usar a funcao uniform.

import random
for x in range(10): # Up to 10 numbers
    print(random.uniform(15,25)) # Number between from 15 to 25 

