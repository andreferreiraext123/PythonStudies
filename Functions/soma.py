# Do a function that sum two numbers and returns the result

# For do a function
    # Put the intruction 'def'
    # The function's name
    # And the parameters

def soma(a,b):
    print(a+b)

# For use a function it's the same way then a commun function, you need put the name and the value

print(soma(10,20)) # Output: 30

# Now writing a sum's function that returns the result
def soma(a,b):
    return a + b

print(soma(10,20))